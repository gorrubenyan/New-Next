import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    devIndicators: false,
    // i18n: {
    //     locales: ['en', 'hy'],
    //     defaultLocale: 'en',
    //     localeDetection: false, // մենք ինքներս ենք վերահղում middleware-ով
    // },
};

export default nextConfig;
